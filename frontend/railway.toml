# Used when the Railway service Root Directory is set to `frontend/`.
# Build is Node + static `serve` only — never Python / Postgres.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# CMD from Dockerfile (serve). Do not set uvicorn here.
healthcheckPath = "/"
healthcheckTimeout = 30
